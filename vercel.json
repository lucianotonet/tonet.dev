{
  "builds": [
    {
      "src": "nuxt.config.ts",
      "use": "@nuxtjs/vercel-builder",
      "config": {
        "postinstall": "npm_config_platform=linux npm_config_arch=x64 npm rebuild rollup --no-save"
      }
    }
  ]
} 